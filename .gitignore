# --- Python / backend ---

# Local virtualenvs
.venv/
api/.venv/

# Bytecode / cache
__pycache__/
*.pyc

# Lambda build artifacts
api/build/
api/deploy.zip
deploy.zip

# --- Node / frontend ---

web/node_modules/
web/dist/

# --- Env files ---

# Backend envs - do NOT commit
api/.env
api/.env.local
api/.env.development
api/.env.test

# Frontend envs - do NOT commit
web/.env
web/.env.local
web/.env.development
web/.env.test

# Allow production env for frontend (safe, only base URL)
!web/.env.production

# --- OS/editor junk ---

.DS_Store
